# Versões mínimas (sem trava exata) — garante que o pip baixe binário
# pronto (wheel) em vez de tentar compilar do zero, o que evitaria precisar
# de Visual Studio Build Tools no Windows.
fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.20
anthropic>=0.69
pandas>=2.3
numpy>=2.0
openpyxl>=3.1
python-docx>=1.1
python-pptx>=1.0
pdfplumber>=0.11
pydantic>=2.10
bcrypt>=4.0
